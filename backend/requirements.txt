# ── BACKEND (FastAPI) ── runtime deps
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
openai>=1.30
hindsight-client>=0.1            # memory SDK (retain/recall/reflect)
pytest>=7.4                      # tests/test_backend.py — `cd backend ; python -m pytest tests/`
# OpenClaw is a Node CLI (not pip) — install separately:
#   npm install -g openclaw  &&  openclaw plugins install @vectorize-io/hindsight-openclaw
# The backend drives it via subprocess (see app/integration/agent/openclaw_agent.py).

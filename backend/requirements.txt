# ── BACKEND (FastAPI) ── runtime deps
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
openai>=1.30
# Hindsight + OpenClaw SDKs — pin once the team confirms the Python packages.
# hindsight-sdk
# openclaw
